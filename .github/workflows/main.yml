name: Test in Docker

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Get dependencies
      run: go mod download

    - name: Run tests
      run: |
        go test ./... -v

    - name: Clean Up
      run: